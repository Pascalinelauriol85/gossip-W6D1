
<div class='container m-5 p-4'>
  <% if flash[:messages] %>
    <div class="container p-1 text-center alert  alert-danger" role="alert">
      <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
      <ul>
        <% flash[:messages].each do |message| %>
          <li><%=message%></li>
        <% end %>
      </ul>
    </div>
  <% end %>


<div class="jumbotron text-center mt-5" style="border-top: 40px white solid;">
    <div class="container">
      <h1 class="display-4">Crée ton <span class='pink'>Gossip</span> !</h1>
      <p class="lead">Racontes moi les potins !!!!</p>
   
   
<%= form_for @gossip do |f| %>

   <div class="form-group">
       <label> Titre du Gossip: </label>
       <%= f.text_field :title, class: 'form-control' %>
    </div>

    <div class="form-group">
      <label> Contenu du Gossip: </label>
       <%= f.text_area :content, class: 'form-control' %>
    </div>

  
    <div class="form-group">
       <button type="submit"> Créer le potin </button>
    </div>

<%end%>

  
</div>